from the app folder run a python interface

then

``` from binance import Binance ```
get the object reference
``` binance = Binance.from_env_file(<path_to_.env_file>) ```
and run an end point with params
``` binance.client.accountDetails(<params>) ```